 npx drizzle-kit push // formerly (npm run db:push) {push schema to database to create or update the table in the database}


 npm run db:seed    //{push seed script to database}

 npm run db:reset    //{reset/clear the database}


 npm run db:studio   //{run and open Drizzle studio locally}

 //        locked: "bg-neutral-200 text-primary-foreground hover:bg-neutral-200/90 border-neutral-400 border-b-4",

 //        rounded: "rounded-full"

 //{/* <Icon
                                        className={cn(
                                            "h-10 w-10",
                                            locked ? "fill-neutral-400 text-neutral-400 stroke-neutral-400": "fill-primary-foreground text-primary-foreground",
                                            isCompleted && "fill-none stroke[4]"
                                        )}
                                    /> */}
